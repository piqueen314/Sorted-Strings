# Sorted-Strings
A small Windows Forms program that sorts user input text in a list box

An app that allows the user to enter strings in a TextBox. Each string input is added to a ListBox. 
As each string is added to the ListBox, ensure that the strings are in sorted order.
